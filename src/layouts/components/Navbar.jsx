import ScreenLayout from "../ScreenLayout";

const Navbar = () => {
  return (
    <section className="bg-white border-b py-6 sticky top-0">
      <ScreenLayout>
        <section className="text-[#FF385C] font-semibold text-2xl">
          airbnb
        </section>
      </ScreenLayout>
    </section>
  );
};

export default Navbar;
